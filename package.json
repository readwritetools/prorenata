{
  "name": "fuse",
  "version": "1.0.5",
  "description": "copy files from two or more source directories into one destination directory",
  "private": true,
  "main": "./dbg/main.class.js",
  "author": "Joe Honton",
  "license": "CC-BY-NC-ND 4.0",
  "scripts": {
    "build": "npm run exim && npm run app",
    "exim": "C:/neonave/lib/exim/exim ./src ./dbg main.class.js",
    "app": "node ./bin/fuse ./test/build-appA.fuse",
    "app-inspect": "node --inspect-brk=9229 ./bin/fuse ./test/build-appA.fuse",
    "bump-version": "npm version patch && git push && git push --tags"
  },
  "dependencies": {
    "bluephrase": "git+ssh://root@nk021:/keep/bluephrase#*",
    "joezone": "git+ssh://root@nk021:/keep/joezone#*"
  },
  "bin": {
    "fuse": "./bin/fuse"
  }
}
